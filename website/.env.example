# Lumen 官网（静态 SPA）构建期环境变量 —— 仅 VITE_ 前缀、均为**非机密**。
#
# 架构：EdgeOne Pages 仅静态托管 dist/（无 Pages Functions、无 KV、无 secret）。
# 全部后端（资源服务器 + OIDC 登录中介）在 Go 服务端 chat.example.com；
# client_secret / refresh_token 仅存于 Go 服务端，绝不进前端产物/仓库/桌面。
#
# 用法：
#   - 本地开发：复制为 `.env.local`（已 gitignore），Vite 自动注入。
#   - 生产/预览：在 EdgeOne 控制台构建环境变量中设置（或写入 edgeone.json）。
#
# 域名 example.com / chat.example.com 为占位，保持可配置——绝不硬编码真实域名。

# Go 后端（资源服务器 + OIDC 登录中介）基址：账户中心/认证跨域直连此处。
VITE_API_BASE_URL=https://chat.example.com

# 下载清单直连地址（Go 服务端公开 GET）。
VITE_UPDATES_LATEST_URL=https://chat.example.com/updates/latest.json

# 官网基址（展示用）。
VITE_WEB_BASE_URL=https://example.com
